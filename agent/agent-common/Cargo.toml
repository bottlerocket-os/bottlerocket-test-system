[package]
name = "agent-common"
version = "0.0.4"
edition = "2021"
publish = false
license = "MIT OR Apache-2.0"

[dependencies]
model = { version = "0.0.4", path = "../../model" }
snafu = "0.7"

[dev-dependencies]
tempfile = "3"
