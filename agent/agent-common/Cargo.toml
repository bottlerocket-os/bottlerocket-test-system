[package]
name = "agent-common"
version = "0.0.9"
edition = "2021"
publish = false
license = "MIT OR Apache-2.0"

[dependencies]
testsys-model = { version = "0.0.9", path = "../../model" }
snafu = "0.7"

[dev-dependencies]
tempfile = "3"
